#include <algorithm>
#include <cmath>
#include <iostream>
#include <numeric>
#include <ranges>
#include <vector>

using namespace std;
int main() {
    vector v{1, 2, 3, 4};
    // for (auto i : v)
    //     cout << i << endl;

    // int sum = 0;
    // for_each(v.begin(), v.end(), [&](int x) { sum += x; });
    // cout << sum << endl;

    // int num = reduce(v.begin(), v.end());
    // cout << num << endl;

    // for(auto &&vi:v
    //     | views::filter([](auto &&x){return x > 0;}){
    //     cout << vi << endl;
    //     }

    return 0;
}
